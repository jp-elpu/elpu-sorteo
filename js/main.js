// ==========================================================
// main.js — punto de entrada
// ==========================================================

import { initConfigPage } from './ui.js';

document.addEventListener('DOMContentLoaded', () => {
  initConfigPage();
});
